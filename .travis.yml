language: php

php:
  - 5.3
  - 5.4
  - 5.5

before_script:
  - git config --global user.name Wellington
  - git config --global user.email wellington30071997@gmail.com

script: phpunit --configuration phpunit.xml --coverage-text